# 专家知识表 3：QA 失败-修复动作映射表

> Phase 3 与 Kimi 共同填写。用途：physics_qa 节点失败后，retune_decide 节点据此选择修复策略（条件边回退到哪个 extract 节点）。

| QA 检查项 | 失败现象 | 可能原因 | 修复动作 | 回退到节点 |
|---|---|---|---|---|
| 单调性 | I-V 曲线非单调 | （待填） | | |
| 对称性 | 漏源互换不对称 | | | |
| kink 检测 | 曲线出现异常拐点 | | | |
| 参数物理范围 | 某参数超出合理区间 | | | |

## 附表：仿真/数据面"签名自检"失败-修复映射（练兵期积累，同样进 Agent 知识库）

> 用途：Agent 调用仿真工具拿数据时，先做**签名自检**（预期现象写死在工具文档里），不符合就按下表诊断，而不是直接拿数据去提参。

| 签名自检项 | 失败现象 | 确诊原因 | 修复动作 | 首次发现 |
|---|---|---|---|---|
| 自热签名（DC 输出饱和区下垂） | 曲线平线、下垂缺失 | 热节点 dt 被接地，ΔT 被钳 0，shmod/rth0 全部失效（`Pwr(rth) <+ Temp(rth)/rth0`，接地则 Temp(rth)≡0） | 网表中 dt 改挂独立节点（如 `N1 d g 0 0 dt yimod`），外部不接电阻——内部 rth0 就是全部热阻 | 2026-09-16 任务07③ |

