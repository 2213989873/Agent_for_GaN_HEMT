# 专家知识表 1：提参顺序表

> Phase 3 与 Kimi 共同填写。记录"哪个模块、先提哪些参数、锁定哪些、依据什么曲线段"。
> 这是 Agent 决策的核心知识，来源于 ASM-HEMT 论文 + Phase 2 手动 pipeline 的实践经验。

| 步骤 | 模块 | 数据 | 提取参数 | 锁定参数 | 依据曲线段 | 备注 |
|---|---|---|---|---|---|---|
| 1 | 直流 I-V | 线性区转移特性 transfer（Vd=1V，Id-Vg） | **voff** | — | Id 起流点 / 阈值过渡区（曲线平移量 = voff 误差，一一对应） | 任务09实测：单参数一轮收敛，NRMSE 0% |
| 2 | 电容 | 准静态 C-V（斜坡法 C=-i/(dV/dt)，-6→6V） | **tbar**（饱和段高度 ∝1/tbar，±20%→12~17% NRMSE）；voff 复验（±0.2V→6.5%） | u0 / rontr1 / rth0（CV 不可见，锁定） | 饱和段高度→tbar；拐点位置→voff | M3 标定：cgso/cgdo 为 F/m 归一化，w=200µm 下 0.002fF（0.001% 噪声级）**不可辨识，不进提取空间**；锁定机制防上一形态参数被拖走（bing 实测 u0 补偿漂移） |
| 3 | 陷阱/自热 | 脉冲 I-V | | | | |
| 4 | 射频寄生 | S 参数 | | | | |
