# 任务卡 03：器件知识速成 + 第一次看懂 I-V 曲线

- **阶段**：Phase 1 · 线 B（器件知识）+ 线 C（数据能力）
- **布置时间**：2026-09-15（任务 02 验收通过后）
- **时间预算**：4–6 小时
- **前置条件**：任务 02 通过 ✅

## 为什么

Phase 2 你要造数据、Phase 3 你要设计"看图决策"的 Agent——看不懂曲线，一切都是空中楼阁。本任务把你从"没摸过曲线"带到"能指认关键区域"。

## 步骤

### ① 读讲义（30 分钟 × 2 遍）

`docs/器件知识速成.md`，第一遍通读，做完练习回头再读一遍。

### ② 画图练习（`examples/03_plot_iv.py`）

用我生成的合成数据 `examples/sample_iv.csv`（205 行，一个虚拟 GaN HEMT 的输出特性，含自热效应）：

1. **输出特性族**：5 条 Id-Vds 曲线（Vgs = 2/3/4/5/6V）画在一张图上
2. **标注**：在图上标出线性区、饱和区、自热下弯的位置
3. **转移特性**：取 Vds = 5V 的数据画 Id-Vgs 曲线，从图上估读 Vth（拐点切线与横轴交点）
4. **回答**：估出的 Vth 是多少？（心里记住，验收要考）

### ③ 思考题（口答给 Kimi）

1. 如果赛题要你"只用一个数据源提 Vth 和迁移率"，你选哪个？为什么？
2. 脉冲 I-V 和直流 I-V 的差异来自哪两个物理效应？
3. 为什么参数提取要"分模块、锁一组提一组"，而不是 120 个参数一起调？

## 验收标准

1. 两张图的 PNG 文件 + 绘图脚本
2. Vth 估读值
3. 三道思考题的回答

## 给 Kimi Code 的提示词（复制即用）

> 在 examples/ 下新建 03_plot_iv.py，用 pandas + matplotlib 处理同目录的 sample_iv.csv（列为 vgs, vds, id，一个虚拟 GaN HEMT 的输出特性数据）：
> 1. 画输出特性族：5 条 Id-Vds 曲线（Vgs=2/3/4/5/6V），不同颜色，图例标注 Vgs 值，坐标轴带标签和单位（Id 单位 A/mm）
> 2. 在图上加文字标注："线性区"（左下斜坡区）、"饱和区"（平台区）、"自热下弯"（高功率端）
> 3. 保存为 examples/output_iv.png，dpi=150
> 4. 再画转移特性：取 vds=5.0 的行画 Id-Vgs 曲线，保存为 examples/transfer_iv.png
> 5. 用代码在转移曲线上做线性区切线外推，估算 Vth 并打印（提示：找曲线最大斜率点做切线，与 x 轴交点即 Vth）
> 服务器是无图形界面的 Linux，用 matplotlib 的 Agg 后端（matplotlib.use("Agg")）。中文注释。
> 写完运行，向我解释切线法估 Vth 的原理。
