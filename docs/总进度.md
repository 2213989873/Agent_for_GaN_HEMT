# 总进度（防失忆总账）

> 本文件取代 `docs/进度快照.md`（原文件保留，git 历史留痕，不再更新）。
> 与 `docs/项目记忆快照.md`（2026-09-18，commit `4da74e9`）互补：那边是细节档案，这边是状态总账。
> 最近更新：2026-09-20（建档）。

## 永久运行纪律（每次开工必读）

1. **每次开工先读本文件**，更新"进行中区"再动手；
2. **每次收工更新状态与证据指针**（commit/文件路径/命令输出）并 git commit；
3. 里程碑翻转为"已验收"**必须附可运行证据**（命令+输出数值+commit），不接受文字汇报；
4. 只填从仓库一手来源（git log、任务卡存档、源码、数据文件）核实过的内容；拿不准的标"未验证"；
5. 答案卡（data/sim/*.inc）与既有归档（data/devices/）勿动；机理结论须过"源码+数据"双重判决。

---

## 1. 大图景速览

赛题（以赛题 PDF 为准；PDF 原件不在仓库，以下转述自 AGENTS.md/总体规划，标"未验证"）：2026 创芯大赛 EDA 精英挑战赛 · GaN HEMT 紧凑模型建模 Agent 赛（概伦电子命题）——测试日 24h 内用 LangGraph + 国产 LLM 经组委会 Primarius Modeling MCP 远程调 MeQLab，给 2 个未知器件全自动提取 ASM-HEMT 约 120 参数。评分 `Card_i = QA通过率 × (1−归一化RMSE)`，`总分 = 0.7×mean + 0.3×min`；红线每器件 MCP 调用 ≥10 次。**测试日日期未公布，每周催组委会。**

**当前阶段一句话**：练兵场已就绪（任务卡 01–15 完成），Agent 本体建设中——架构设计 v1.1 过审（`089ae5c`），**M1 已验收（`3e7196c`）**，下一步 M2。

## 2. 已完成区（按任务卡/里程碑，证据=git log）

| 完成项 | 日期 | 证据指针 | 关键结论一句话 |
|---|---|---|---|
| 项目骨架 + AGENTS.md + 总体规划 | 09-14 | `c96fc5c` / `d4fd206` | 三条总策略定调：QA>贴合、不偏科、稳健>聪明 |
| 任务01–04 Agent 筑基（LangGraph 双 demo / FastMCP 通信 / 器件知识+I-V 画图 / 手写 function-calling 循环） | 09-14–15 | `11d4f87`、`8e03eb7` 等 sync 链；examples/ 7 脚本 | LangGraph state/node/条件边与 MCP Client-Server 握手跑通 |
| 任务05 仿真环境搭建（ngspice-47 自编译 --enable-osdi + OpenVAF + ASM-HEMT） | 09-16 封档 | `docs/任务卡-05-仿真环境搭建.md`（含踩坑10条） | OSDI 链路验证通过 |
| 任务06 第一个虚拟器件 | 09-16 | `e2ee77b` | 5 引脚 (d,g,s,b,dt) 确认；冒烟转移 81 点收敛，峰值 1.591A |
| 任务07 甲乙双器件 + 陷阱死亡案破案 | 09-16–17 | `ea5f313`（处方升 v4） | **rdsmod=0 漏极短接是陷阱无效真凶**（asmhemt.va 493/1054 行）；自热下垂签名 -2.8% |
| 任务07.5 搬迁本地 WSL | 09-16 | `docs/任务卡-07.5-本地环境复刻.md` | 环境复刻对拍逐位一致（峰值 1.59272A） |
| 任务08 C-V 量产（斜坡法） | 09-18 15:47 | `8082404` | S 形曲线 1208 点，饱和 187fF；乙左移 0.2V（预期"右移"证伪更正） |
| 任务09 最小提取闭环 + 三件套 | 09-18 15:54 / 16:11 | `1522525` / `727d60d` | 单参数 voff 一轮收敛 0%；三件套=配对历史/二分策略/最优交卷/temperature=0 |
| 任务10 双参数耦合三幕 + 混合架构定型 | 09-18 16:37 | `8ba2651` | LLM 粗调+least_squares 精修；**真值不在子集空间**（真值 11.08% vs 假最优 9.88%） |
| 任务10-bis 外部评审四任务 | 09-18 17:02–17:03 | `f16525f` / `880f893` | 归因回填（残差集中 Vg∈[0,2] 18.81%）；规范3 实证（补 rontr1 → 0.0003% 全命中）；外推反弹 2×；QA 阈值定稿 |
| 任务11 physics_qa 闭环 | 09-18 17:16 | `0c277b3` | 驳回→重调→自动扩空间全自动走通，0.0002% 命中；补偿解吸引盆换初值逃不掉 |
| 任务12 双器件泛化 + 输出特性 847 点 | 09-18 17:24 | `b52a5d9` | 甲 expand 无误触发（0.0000%/6 次）；发现 QA 漏网补偿解（u0=0.1305）→ 催生触发器2 |
| 任务13 热参数族 + 双触发器 | 09-18 17:32 | `d78bad1` | 三参数 0.60%/15 次 vs 四参数 0.0003%/349 次——RMSE_TARGET=1% 是"够用 vs 追真"旋钮 |
| 任务14 MCP 切换演练 | 09-18 18:01 | `8558620` | sim_fn 一行换协议，结果零偏差（0.6002%），调用计数 64 次过红线 |
| 任务15 测试日作战手册 | 09-18 19:12 | `784f770` | 24h 七时间块 + 熔断 A–D + 战前检查单定稿 |
| 记忆固化快照 | 09-18 19:04 | `4da74e9` | 十节细节档案（源码行号/数值档案/证伪清单） |

## 3. 已证伪清单（防假说复活）

| 假说 | 推翻实验 | 证据指针 |
|---|---|---|
| 陷阱签名为零是因没给应力相 | tran 从 DC 工作点启动=无限长应力；真凶 rdsmod=0 | 任务07④-bis；`data/devices/probe_rdsmod.sp`；`ea5f313` |
| 换编译器（openvaf-r）能让陷阱生效 | 重编后签名仍为零 | 2026-09-17 实测；`asmhemt.osdi.bak-devel2023` |
| rontr1 参数不存在/被忽略 | 假参数 rontr99 报 unrecognized，rontr1 无警告 | 2026-09-17 probe_bogus 输出 |
| C-V 乙相对甲"右移" | 实测左移（voff 更负→同 Vg 沟道更强→电容更大） | `8082404` 任务卡08 存档更正 |
| 裸 LLM 坐标下降能收敛双参数 | 鞍点死锁（voff -2.0↔-2.1 循环） | `8ba2651` 任务卡10 第二幕 |
| 换初值能逃补偿解吸引盆 | 两次初值同收敛 (-2.52, 84e-3) | `0c277b3` 任务卡11 |
| 无配对历史 LLM 能稳定收敛 | 震荡停 9.48% vs 三件套后一轮收敛 | `727d60d` 任务卡09 |
| （旧服务器）openvaf-r 可用 | glibc 2.39 判死 | 旧进度快照§4（本地 WSL 可用，已实测） |

## 4. 进行中区

- **当前任务**：Agent 本体开工——`docs/agent架构设计.md` 已交评审（2026-09-20），**审过之前不进 M1**。
- **当前任务**：M1（`3e7196c`）、M2（`cfec4da`）、M3（`dc4aacc`）、M4（`d1bf153`）已验收。
- 阻塞项：① 测试日日期未公布（每周催组委会）；② Qwen/GLM 备用 key 未填（战前检查单硬项）；③ 赛题 PDF 原件不在仓库，赛题事实均为转述（标"未验证"）。
- 下一步：M5——双器件连跑 + 提交物生成（开工时先处置下方 M3 遗留的"跨形态一致性"：finalize 前加联合精修节点）。
- **M3 遗留发现（M5 前须处置）**：**跨形态一致性**——bing 演示中 u0 在转移形态被未建模的 tbar 差异补偿到 0.1299（0.35% 过了 1% 达标线），CV 形态修好 tbar 后 u0 仍带补偿值，最终模型卡"CV 完美/转移回潮"。拟在 M5 finalize 前加"联合精修"（全参数在关键形态复抛）。
- **M1 两个发现（已处置）**：① run_qa.py 存在历史 lambda 关键字 bug（`lambda p,t` vs 调用端 `tag=`），HEAD 上必崩——卡12 Part A 的 run_qa 验收记录标"存疑"，已修复并回归复证（0.0000%/0.0002% 与原记录一致）；② **仿真计数口径**：qa_loop 日志的"nfev 次仿真"不含数值雅可比调用（trf 口径），实际 ngspice 次数≈nfev×(1+参数数)——M1 甲 nfev=6 实为 18 次；历史"仿真成本"账（13/42/349 次）均为 nfev 口径，红线计数以接口层实际调用为准（更宽松，不影响过线）。

## 5. 待完成区（通往完整 Agent 的里程碑表）

> 与 `docs/agent架构设计.md` §5 一致；两份文件任何一份改动，另一份必须同步。

| 里程碑 | 内容 | 验收判据（跑什么命令、看什么数值） | 状态 | 验收证据 commit |
|---|---|---|---|---|
| **M1** | **甲器件最小全流程无人干预跑通**（主图+extract 子图+checkpoint，2–3 参数族 voff/u0[/rontr1]） | `python src/pipeline/run_agent.py --device jia` 全程无人干预；NRMSE ≤0.01%（卡12 基准 0.0000%/6 次仿真）；QA 全过；日志含预算计数；kill 后 `--resume` 续跑成功 | **已验收**（2026-09-20：NRMSE=0.0000%，QA ✅，调用计数 18≥10；timeout 4s 强杀于 load_data 后，`--resume` 跳过已完成节点跑完，结果一致；回归 run_qa jia 0.0000% / yi 0.0002% 三参数命中） | `3e7196c` |
| M2 | 乙器件全流程（自热+陷阱，转移+输出两形态轮转） | `run_agent.py --device yi` 无人干预；NRMSE ≤0.6%（卡13/14 基准）；调用计数 ≥10 且落盘自证；双触发器日志可审 | **已验收**（2026-09-21：`--forms dc_transfer,dc_output`；NRMSE=**0.0004%**，四参数全命中 voff=-2.2/u0=0.14999/rontr1=-0.99987/rth0=19.985；计数 2001 次；触发器1（QA 驳回×2→扩 rontr1）+触发器2（0.60% 未达 0.3%→扩 rth0）日志全记录；回归三入口逐位一致） | `cfec4da` |
| M3 | C-V 形态纳入提取（电容族，知识表1/2 回填） | `--device jia --forms dc_transfer,cv` 双形态闭环；C-V NRMSE 达标线开工时标定（未验证）；知识表回填 commit | **已验收**（2026-09-21：标定实验定 tbar=电容族、达标线 0.5%（噪声底 0.0035%）；jia `--forms dc_transfer,cv_gg` 0.0000% 闭环；丙器件（tbar=3.2e-8）演示触发器2 扩 tbar 命中真值；知识表1/2 电容行回填 `dc4aacc`；修复 clip 夹反/x_scale 量纲/分形态锁定三处） | `dc4aacc` |
| M4 | MCP 链路全流程 | `run_agent.py --device yi --via-mcp` 与 M2 本地直连一致（零偏差基准 0.6002%）；计数 ≥10 | **已验收**（2026-09-22：`--device yi --via-mcp --forms dc_transfer,dc_output` NRMSE=**0.0030%**、sim_count=**1534 与 M2 本地直连逐位一致**（轨迹逐步相同），34.7s 长连接零感知开销；server v2 dict 通道+McpSession 长连接（后台线程+专属 loop）；回归 run_qa jia 0.0000% / run_agent jia 0.0000%/18次 / run_qa_mcp 0.6003%（存档 0.6002%，Δ0.0001pp=LLM 粗调路径噪声）/59次） | `d1bf153` |
| M5 | 双器件连跑 + 提交物生成 | `run_agent.py --devices jia,yi` 一条命令；两份模型卡+日志包产出；Schema 校验通过 | 未开始 | —— |
| M6 | 24h 作战 dry run | 作战手册 T0–T5 全块演练入档；熔断 A–D 各模拟触发一次有处置证据 | 未开始 | —— |
