# 任务卡 10：双参数耦合提取 —— 第一次面对"参数打架"

- **阶段**：Phase 3 · 提取架构的关键一跃
- **布置时间**：2026-09-18
- **前置条件**：任务卡 09（单参数闭环 + 三件套）
- **状态**：✅ 已完成（三幕实验 + 混合架构定型）

## 目标

从单参数升到两参数（voff + u0 同时提），目标器件=乙（真值 voff=-2.2, u0=150e-3）。
学会建模的真正难题：参数效果互相纠缠，单看曲线分不清谁错了。

## 三幕实验实录（全部实测，无一行猜测）

### 第一幕：裸 LLM 双参数（run_dual.py v1）
NRMSE 单调下降 11.4%→7.86%（三件套生效），但 10 轮卡在假最优
voff=-2.10/u0=135e-3：voff 偏正导致起流偏早，u0 被压低背锅补偿。
**症状**：LLM 反复念叨"起流点在 -2.5~-2 之间"——采样点太稀，判断不了精确位置。

### 第二幕：加密采样 + 坐标下降铁律（v2）
过渡区采样加密后 LLM 首轮就锁定"起流点 -2.4~-2.3"（分辨率提升✅），
但"每轮只改一个参数"的铁律把它锁死在 -2.0↔-2.1 死循环，u0 全程冻结——
**单变量下降在非凸误差面上卡进鞍点**：u0=0.17 偏高时，真值 voff=-2.2 的
NRMSE(14%) 反而比 voff=-2.0(11.4%) 差，坐标下降必然回头。

### 事实核查掀桌（转折点）
```
真值组合 (voff=-2.2, u0=0.15):  NRMSE = 11.08%  ← 不是 ≈0！
         (voff=-2.1, u0=0.15):  NRMSE =  9.88%  ← 比真值还好
```
**真值不在 (voff,u0) 两参数张成的空间里**：乙还带 trapmod=2+rontr1=-1，
ron_trap 随 Vg 变化（~0.9-1.25Ω），改变了曲线形状，平移/缩放无法补偿。

### 第三幕：混合架构定型（run_hybrid.py）
```
A. 乙器件：LLM 粗调 3 轮(11.42%) → least_squares 精修
   → NRMSE 2.70%（13 次仿真），但 voff=-2.52/u0=84e-3 远离真值！
B. 干净目标（只差 voff/u0 的合成器件）：7 次仿真 NRMSE=0.0000% 精确命中
```

## 四条核心教训（进 Agent 设计规范）

1. **LLM 粗定位 + 优化器精修 = 工业标准架构**。LLM 负责归因和初值，
   精细收敛交给 least_squares。120 参数的赛题不可能靠 LLM 逐点猜。
2. **RMSE 与参数真值会脱钩**（欠辨识/过参数化补偿）：2.70% 的解参数全错。
   这正是赛题评分"QA通过率 × RMSE"里 QA 存在的意义——**必须加参数物理范围检查**，
   否则优化器交出 u0=84e-3 这种曲线对但物理荒谬的解。
3. **子集提取精度上限 = 未建模差异**。提 voff/u0 时若器件还有陷阱差异没进
   提取空间，残差钉死。提取空间要覆盖主要差异源，或接受 RMSE 残差。
4. **测量分辨率决定提取精度**（第一幕）：观测协议本身是提取质量的上限。

## 产物

- `src/agent/dual_loop.py` — 双参数 LLM 闭环（v2 坐标下降版，教训存档）
- `src/pipeline/run_dual.py` — 双参数入口
- `src/pipeline/run_hybrid.py` — **混合架构（定型方案）**
- scipy 1.18.1 已入 gan-agent 环境

## 思考题

1. 为什么干净目标能 0.0000% 命中而乙不能？（提示：看乙处方里还有什么开关开着）
2. 如果赛题 QA 检查"u0 必须在 100~200e-3"，混合架构里该在哪一步加这个检查？
3. 乙的 ron_trap 要进提取空间的话，提取参数从 2 个变 4 个（+rontr1, +a1），
   least_squares 的仿真成本怎么变？（提示：数值雅可比的维度）

## ✅ 任务卡 10-bis 存档（2026-09-18，外部评审追加，全部实测）

### 任务 1：rdsmod 归因回填
harness 原文（`src/tools/sim_tools.py:53`）：`.model trialmod asmhemt (rdsmod=1 voff={voff} u0={u0})`
目标原文（`data/sim/yi.inc`）：`rdsmod=1` 在首位。
→ 两侧 rdsmod=1 一致，**接入电阻基线差 = 0**；缺口全在 trapmod=2 的 ron_trap。
定量分解（真值组合试算 vs 乙目标，分段残差）：
深耗尽段 0.00% → Vg∈[-2,-1) 0.28% → Vg∈[-1,0) 4.48% → **Vg∈[0,2] 18.81%**。
机制：侵蚀压降 = Id×ron_trap ∝ Id，高栅压段 Id 大，残差在此爆发；
ron_trap 本身 0.9~1.4Ω 随 Vg 缓变。原"天花板 9.9%"归因成立，无需修正。

### 任务 2：规范 3 实证（提取空间补 rontr1，重跑混合架构）
```
初值 (voff=-2.1, u0=0.155, rontr1=0)，least_squares 42 次仿真：
voff=-2.2000 (真值-2.2) ✅  u0=149.99e-3 (真值150) ✅  rontr1=-0.9999 (真值-1) ✅
NRMSE = 0.0003% << 1%
```
预登记判读通过：**规范 3（提取空间须覆盖主要差异源）实证成立**。
派生判据：某族参数的"进场时机" = 当前提取空间残差撞天花板之时（120 参数分区提取预演 ✅）。

### 任务 3：外推风险量化（extrap_check.sp，补偿解 vs 真值乙）
| 网格 | NRMSE | 相对拟合集 |
|---|---|---|
| 拟合集内（Vd=1V @27°C 转移） | 2.70% | 基准 |
| Vd=4V 转移（未拟合偏置） | 5.59% | 反弹 2.1× |
| Vd=8V 转移（更远偏置） | 5.48% | 反弹 2.0× |
| Vd=1V @85°C（温度外推） | 2.72% | ≈持平（温度系数两侧皆默认，缩放规律一致） |
结论：补偿解在偏置外推网格上误差翻倍——赛题隐藏 (Vgs,Vds) 网格会直接吃掉这类解的 RMSE 分。
**规范优先级定稿：扩提取空间（治本）> QA 物理范围（兜底）> 补偿解（须过 QA + 记录外推风险，仅最后手段）**。

### 任务 4：知识表回填
a) **效率账**：LLM 粗调 3 轮（3 仿真+3 LLM 调用）；优化器精修 2 参数 13 次、
   3 参数 42 次、干净目标 7 次仿真；干净目标全程 <10s——混合架构效率量化证据。
b) **移交点判据**（可执行）：粗调出现"相邻两轮 NRMSE 改善 <3 个百分点"或
   NRMSE ≤15% → 移交 least_squares 精修。
c) **QA 物理范围**（可执行，已入知识表 3）：
   u0∈[100e-3, 250e-3] m²/(V·s)（GaN 低场迁移率典型 1000–2500 cm²/V·s；84e-3 触发告警）；
   voff(D-mode)∈[-4, -0.5] V；rontr1∈[-3, 1]；通用规则：优化器解触边界=告警。
